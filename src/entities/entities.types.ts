export type Contact = {
    text: string
    subtext: Array<string>
}

export type Service = {
    title: string
    description: string
    iconPath: string
};

export type SitemapLink = {
    text: string
    href: string
}